# 变更记录

## 2.1.0-rc1 — 关闭系统定位时的持续回调候选版

- 参考用户提供 GhostMapX 2.3.2 的部分静态定位行为，独立实现 Android 12+ 系统持续监听器补充派发。
- 真实系统定位关闭时，为满足权限及 AppOps 条件的应用补发模拟位置；系统定位开启时继续使用既有替换路径。
- 增加定位可用性查询适配及缓存失效处理，不修改真实定位开关；处理停止、取消、Binder 死亡、请求间隔、次数及时限。
- 包含 2.0 后续的初始化异常隔离及系统备份/换机迁移排除规则。
- 一次性请求、PendingIntent、仅粗略权限和私有 SDK 通道未完成主动派发，不能承诺全应用免弹窗或不存在真实位置来源。
- 使用与 2.0 相同的正式证书构建预发布安装包；尚需 LSPosed 真机验证。详见 docs/GHOSTMAPX_REVIEW.md。

## 2.0.0 — 正式签名与稳定性改进

- 参考原版 2.0.2 的部分定位实现，独立改进 Hook 初始化异常隔离；系统进程不再误装应用适配。
- 系统 LocationResult 为接收方生成独立副本，避免共享对象在不同应用坐标系间串改。
- 修复配置监听初值的竞态、无效坐标启用模拟、零海拔和零速度参数不生效问题。
- 恢复仅选择三个系统组件的全局系统接口模式，移除 1.2.6 强制选择应用的限制。此前日志不能证明失败必由 SDK 覆盖导致；私有 SDK 等通道仍需实测。
- 移除类加载器和安装路径日志，关闭系统备份，地图凭据继续由用户自备。
- 使用独立 RSA 3072 正式签名；从 Debug 迁移须保存数据后卸载旧版，后续保持同签名升级。缺少正式密钥时不再回退 Debug 签名。
- 固定与 Kotlin 2.2 兼容的 R8 8.10.21。2.0 尚未完成 LSPosed 真机验证，不承诺全应用覆盖或无法检测；详见 docs/V2_REVIEW.md。

## 1.2.6 — 系统级 Hook 作用域组合修复（预发布）

- 根据 1.2.5 真机日志确认：系统服务已经替换微信、支付宝、京东、高德和美团的 Android 定位返回，但只有高德和美团采用该结果；微信、企业微信、支付宝、京东等应用仍可能由进程内厂商 SDK 产生最终位置。
- 恢复系统级模式的正确含义：在目标应用的应用级 Hook 基础上，再增加 System Framework、Android System 和 Phone Services；目标应用本身仍须位于 LSPosed 作用域。
- 系统级模式现在至少需要一个目标应用；只有系统组件时拒绝开启并显示明确提示。
- 升级自 1.2.5 后，若作用域中没有目标应用，会自动退出无效的系统级模式；移除最后一个目标应用时也会自动退出。
- 保留 1.2.5 已验证能命中第三方调用者的系统服务替换，以及 1.2.4 已验证可用的微信/企业微信应用进程适配。

## 1.2.5 — 系统级 Hook 目标识别修复（预发布）

- 修复系统级 Hook 错把 LSPosed 系统作用域列表当作应用白名单的问题；仅勾选 System Framework、Android System 和 Phone Services 时，第三方应用现在也会进入系统定位替换流程。
- 从 Android 系统服务的调用参数或定位注册对象中识别实际接收应用，并应用该包名对应的坐标系；没有单独配置时使用 WGS-84。
- 补充 Android 15 `GetCurrentLocationListenerRegistration` 一次性定位回调入口。
- 增加首次替换诊断日志及系统包过滤测试，避免修改纯系统内部定位投递。
- 保留 1.2.4 已经真机验证成功的微信和企业微信应用级 Hook。

## 1.2.4 — 企业微信 Play 5.0.9 sapp SDK 适配（预发布）

- 根据 5.0.9 / 75141 安装包确认页面使用 sapp.TencentLocation，补上该命名空间的腾讯 SDK 接口适配。
- 保留 1.2.3 已在用户微信测试中生效的 Android 回调。
- 新增 sapp 发现回归测试及 SDK 命中/回调日志；企业微信页面结果待真机确认。

## 1.2.3 — 定位主动回调（预发布，待真机验证）

- 补充 Android LocationManager 监听器注册/移除适配：保留正常注册，在模拟启用且已选点时每秒补发新位置，使用调用方 Executor/Looper。
- 处理 getCurrentLocation 的 Consumer 回调和取消信号；停止模拟后不派发旧的模拟位置。
- 应用初始化完成后重试腾讯 SDK，按 Method 去重，避免重复安装。
- 增加开关状态、选点是否存在及回调送达日志，不记录经纬度和地图密钥。
- 对监听器身份、移除和过期排队任务增加单元测试。
- 1.2.2 的腾讯 SDK 启动时探测在用户反馈环境未命中；此前版本说明不代表微信/企业微信已实测通过。

## 1.2.2 — 应用级 Hook 与腾讯定位兼容

- 修复定位模式状态计算错误；关闭系统级 Hook 后现在可以正确选择并显示“应用级 Hook”。
- 新增腾讯定位 SDK 回调适配，替换微信、企业微信读取的 `TencentLocation` 经纬度及坐标类型。
- 适配腾讯 SDK 的持续定位、单次定位、最后位置和移除监听器流程，保持原有错误码与地址信息。
- 将应用定位 Hook 提前到包类加载器就绪阶段安装，避免错过微信启动期间注册的定位监听器。

## 1.2.1 — 企业微信定位兼容性修复

- 系统级 Hook 不再吞掉异步 `getCurrentLocation` 请求，保留系统定位回调并在派发阶段替换位置。
- 应用级和电话服务 Hook 不再默认清空 Wi-Fi、基站信息或阻止 GNSS 回调注册，避免腾讯、高德等厂商定位 SDK 无法建立定位结果。
- 新构造的位置包含有效精度、当前时间和单调时钟时间，避免 fused/腾讯定位客户端丢弃不完整或过期位置。
- 保留每应用 WGS-84、GCJ-02、BD-09 输出；企业微信可继续单独选择 GCJ-02。

## 1.2.0 — 多地图供应商

- 新增百度、高德和 Google 地图供应商切换。
- 高德支持 Web Key 与 `securityJsCode`；Google 支持 Maps JavaScript、Places 和 Geocoding API Key。
- 地图选点、搜索结果和反向地址解析统一转换并保存为 WGS-84，切换地图不会改变收藏位置。
- 地图凭据保存在应用私有本地配置中，可随时更换并刷新地图。

## 1.1.0 — 性能与能耗优化

- 优化后台运行机制，退至后台后自动释放地图 WebView、脚本及渲染资源。
- 优化地址解析与搜索请求调度，减少连续点选产生的重复网络请求。
- 优化 Hook 配置缓存与按需执行逻辑，降低定位高频回调期间的资源开销。
- 优化 Mock Provider 服务生命周期和熄屏推送频率，减少后台唤醒与耗电。
- 移除原生地图 SDK 和非必要资源，并启用代码、资源压缩。

## 1.0.1 — 地图服务与界面优化

- 新增自定义百度地图 AK，可在默认免费额度不足时切换为用户自行申请的浏览器端（JavaScript API）AK。
- 新增 Telegram 频道入口，方便订阅项目动态与交流探讨。
- 优化部分 UI 交互与视觉效果。

## 1.0.0 — 首次公开发布

- 重新实现更简洁、时尚的地图交互界面，整合地点搜索、收藏、当前位置、位置参数配置与作用应用管理。
- 将地图服务替换为百度地图 Web 版本，支持地图选点与地点搜索。
- 整合 HideMockLocation 的部分实现特性，提升本模块在部分反模拟检测较强应用中的位置模拟隐藏性。
- 提供互斥的应用 Hook、系统 Hook 与 Mock Provider 三种模式。

## 上游说明

本项目在 XposedFakeLocation 的基础上维护，并整合了基于 HideMockLocation 的部分实现思路。早期私人开发记录不作为公开版本历史发布；上游归属与许可证见 [docs/FORK_CHANGES.md](docs/FORK_CHANGES.md) 和 [LICENSE](LICENSE)。
